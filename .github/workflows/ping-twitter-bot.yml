name: Ping Twitter bot

on:
  schedule:
    - cron: "*/1 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger bot endpoint
        run: |
          curl -sS https://<your-vercel-project>.vercel.app/api/check-tweets
